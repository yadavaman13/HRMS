{
  "name": "hrms-template-root",
  "private": true,
  "version": "1.0.0",
  "description": "Enterprise-ready HRMS Hackathon Template with CI/CD, linting, testing, and production build pipelines",
  "scripts": {
    "prepare": "husky",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "lint": "npm run lint --prefix client && npm run lint --prefix server",
    "lint:fix": "npm run lint:fix --prefix client && npm run lint:fix --prefix server",
    "test": "npm run test:ci --prefix server",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "eslint": "^10.8.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "prettier": "^3.9.6",
    "stylelint": "^17.14.1"
  }
}
